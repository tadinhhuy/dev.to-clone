import React, { Fragment } from 'react';
import AppRouter from './AppRouter';
import './main.css';

const App: React.FC = () => {
  return (
    <Fragment>
      <AppRouter />
    </Fragment>
  );
};

export default App;
